import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const StyledDomainExperts = styled.div `
    ${tw ``};
`;
const StyledExperts = styled.div `
    ${tw `flex`};
`;
export {
    StyledDomainExperts,
    StyledExperts
}
