import styled from '@emotion/styled'
import tw from 'tailwind.macro'

export const StyledRequests = styled.div`
   ${tw``};
`

export const StyledRequestsTitle = styled.p`
   ${tw`text-orange-500`};
`
