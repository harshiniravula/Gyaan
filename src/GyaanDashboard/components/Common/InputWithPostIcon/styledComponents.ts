import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const StyledWrapper = styled.div`
   ${tw`flex mx-16 items-center justify-start border border-gray-500`};
`
export { StyledWrapper }
