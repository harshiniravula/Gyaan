import styled from '@emotion/styled'
import tw from 'tailwind.macro'

const StyledListTitle = styled.div `
    ${tw `flex justify-between items-center border border-black p-2`};
`;

const StyledTitle = styled.p `
    ${tw `text-sm`};
`;
export {
   StyledListTitle,
    StyledTitle
}
